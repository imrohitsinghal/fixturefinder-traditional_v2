{
  "name": "my-static-site",
  "version": "1.0.0",
  "description": "This will load any static html site",
  "scripts": {
    "start": "harp server --port $PORT",
    "test": "jasmine"
  },
  "dependencies": {
    "harp": "*"
  },
  "devDependencies": {
    "jasmine": "^3.5.0"
  }
}
